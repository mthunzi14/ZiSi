# ZiSi Bot - Python Dependencies
# Install with: pip install -r requirements.txt

# Environment / config
python-dotenv>=1.0.0

# HTTP client
requests>=2.31.0

# Claude AI (sentiment analysis)
anthropic>=0.40.0

# Google Drive + Sheets logging
google-api-python-client>=2.130.0
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Optional: Polymarket CLOB client (uncomment if available via pip)
# py-clob-client>=0.16.0

# Local sentiment analysis (free, no API key needed)
transformers>=4.30.0
torch>=2.0.0
numpy>=1.24.0

# RSA signing for Kalshi API v2 authentication
cryptography>=41.0.0

# ML pipeline — logistic regression calibration + gradient boosted models
scikit-learn>=1.3.0

# Kalshi SDK for order execution
kalshi-python>=2.1.0
