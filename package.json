{
  "name": "zisi-bot",
  "version": "2.0.0",
  "description": "ZiSi Polymarket Up/Down Bot — single-command launcher",
  "private": true,
  "scripts": {
    "start": "npm --prefix presentation/dashboard/frontend run build && npm --prefix presentation/dashboard/backend start",
    "dev": "npm --prefix presentation/dashboard/frontend run build && npm --prefix presentation/dashboard/backend run dev",
    "build": "npm --prefix presentation/dashboard/frontend run build",
    "install:all": "npm --prefix presentation/dashboard/frontend install && npm --prefix presentation/dashboard/backend install"
  },
  "dependencies": {
    "@azure/mcp-win32-x64": "^3.0.0-beta.15"
  }
}
